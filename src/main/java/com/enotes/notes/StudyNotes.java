package com.enotes.notes;

public class StudyNotes {

	/*
	//ObjectUtils -
	
	Used for null / empty checks on objects & arrays.
	
	Common methods
	ObjectUtils.isEmpty(obj);      // null, empty array, empty collection
	ObjectUtils.nullSafeEquals(a, b);
	ObjectUtils.nullSafeHashCode(obj);
	
	//StringUtils
	
	Used for String validation & manipulation.
	
	StringUtils.hasText(str);     // not null, not empty, not whitespace
	StringUtils.hasLength(str);  // not null, length > 0
	StringUtils.capitalize(str);
	StringUtils.trimWhitespace(str);
	
	//CollectionUtils
	Used For Collections & Maps.
	
	Common methods
	CollectionUtils.isEmpty(list);
	CollectionUtils.isEmpty(map);
	*/
	
}
